# Now it's time to parse

Once you've defined a grammar, you need to parse data with it. **Reccent** only supports parsing
grammars based on character streams, so the data being parsed must be a string.
